config proftpd-in-fit
	bool "reduce proftpd"
	default n
	depends PACKAGE_proftpd
	help
	  Feature: cut off some optionsal features of proftpd.

config proftpd-limit-guest-ubus
    bool "limit guest network access"
    default n
    depends PACKAGE_proftpd
    help 
        Feature: proftpd limit guest network access
